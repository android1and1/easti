// Generated by CoffeeScript 2.3.2
(function() {
  $(function() {
    var help;
    $('form').on('submit', function(e) {
      // ajax do query
      $.ajax({
        url: '/daka/one',
        data: $(this).serialize(),
        type: 'GET',
        dataType: 'json'
      }).done(function(json) {
        var $tbody;
        $tbody = $('.table tbody');
        $tbody.html('');
        return help($tbody, json);
      });
      e.preventDefault();
      return e.stopPropagation();
    });
    return help = function(tbody, arr) {
      var $tr, ele, i, len, results;
// arr -> [{},{},...] ,each {} -> {alias:xxx,utc_ms:xxx,....}
// change each element to '<tr>'
// change each attr of element to '<td>'
      results = [];
      for (i = 0, len = arr.length; i < len; i++) {
        ele = arr[i];
        $tr = $('<tr/>');
        
        // order:['alias','category','timestamp','dakaer']
        $tr.append($('<td/>', {
          text: ele.alias
        }));
        $tr.append($('<td/>', {
          text: ele.category
        }));
        $tr.append($('<td/>', {
          text: (new Date(ele.utc_ms)).toLocaleString()
        }));
        $tr.append($('<td/>', {
          text: ele.dakaer
        }));
        $tr.append($('<td/>', {
          html: '<button class="btn btn-default detailbutton"> Detail</button>'
        }));
        results.push(tbody.append($tr));
      }
      return results;
    };
  });

}).call(this);
