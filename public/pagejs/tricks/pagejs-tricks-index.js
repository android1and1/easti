// Generated by CoffeeScript 2.1.1
(function() {
  $(function() {
    return $('a').on('click', function(evt) {
      var url;
      // do-ajax
      // step1 - get evt.target's url.
      url = $(this).attr('href');
      $.ajax({
        url: url,
        responseType: 'json',
        method: 'POST'
      }).catch(function() {
        return alert('wrong.');
      }).done(function(json) {
        var say;
        // 这里的字符串插值方式很常见，实用！
        say = `id is ${json.id},about is ${json.about},content is ${json.content},moment is ${json.moment},visits is ${json.moment}.`;
        return alert(say);
      });
      return false;
    });
  });

}).call(this);
