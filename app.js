// Generated by CoffeeScript 1.10.0
(function() {
  var PROJECT_ROOT, app, express, http, path, routers, server, spawn, static_root;

  path = require('path');

  http = require('http');

  spawn = require('child_process').spawn;

  express = require('express');

  app = express();

  PROJECT_ROOT = process.env.HOME + '/easti';

  app.set('view engine', 'pug');

  static_root = path.join(PROJECT_ROOT, 'public');

  app.use(express["static"](static_root));

  app.use(express.urlencoded({
    extended: false
  }));

  routers = ['neighborCar', 'readingjournals', 'tools', 'alpha', 'uploading', 'glossary'];

  routers.forEach(function(name) {
    path = './routes/route-' + name;
    return (require(path))(app)('/' + name);
  });

  app.get('/', function(req, res) {
    return res.render('index', {
      title: 'I see You',
      name: 'wang!'
    });
  });

  app.use(function(req, res) {
    res.status(404);
    return res.render('404');
  });

  app.use(function(err, req, res, next) {
    console.error('occurs 500 error. [[ ' + err.stack + '  ]]');
    res.type('text/plain');
    res.status(500);
    return res.send('500 - Server Error!');
  });

  if (require.main === module) {
    server = http.Server(app);
    server.listen(3003, function() {
      return console.log('server running at port 3003;press Ctrl-C to terminate.');
    });
  } else {
    module.exports = app;
  }

}).call(this);
