// Generated by CoffeeScript 2.3.1
(function() {
  var NohmModel, ReadingJournals, pug;

  pug = require('pug');

  NohmModel = (require('nohm')).NohmModel;

  ReadingJournals = (function() {
    class ReadingJournals extends NohmModel {
      static mod2snippet() {
        var abc, firstkey;
        abc = '-\n  if(field_type === \'string\')\n    field_type=\'text\';\n\n.form-group\n  label(for= \'id\' + field_label)= field_label \n  input(type=field_type id= \'id\' + field_label class="form-control" placeholder="you know..")';
        firstkey = (Object.keys(ReadingJournals.definitions))[0];
        return pug.render(abc, {
          field_label: firstkey,
          field_type: ReadingJournals.definitions.title.type
        });
      }

      static getDefinitions() {
        return ReadingJournals.definitions;
      }

    };

    ReadingJournals.version = '1.0';

    ReadingJournals.modelName = 'readingjournals';

    ReadingJournals.idGenerator = 'increment';

    ReadingJournals.definitions = {
      title: {
        type: 'string',
        unique: true,
        validations: ['notEmpty']
      },
      author: {
        type: 'string',
        validations: ['notEmpty']
      },
      timestamp: {
        type: 'timestamp',
        defaultValue: 0
      },
      revision_info: {
        type: 'string'
      },
      journal: {
        type: 'string',
        validations: ['notEmpty']
      }
    };

    return ReadingJournals;

  }).call(this);

  module.exports = ReadingJournals;

}).call(this);
